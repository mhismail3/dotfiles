# macOS
.DS_Store
.AppleDouble
.LSOverride

# Editor
*.swp
*.swo
*~

# Local config (machine-specific)
.dotfiles_config

# API keys (never commit secrets!)
.env.api-keys

# Claude Code (synced files are committed individually, not bulk dirs)
claude/plans/
claude/plugins/
